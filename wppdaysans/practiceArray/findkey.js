const arr = [1, 20, 3, 12, 4]
let key = 20;

for (let i = 0; i < arr.length; i++) {
    // const element = array[index];
    if (arr[i] == key) {
        console.log(true);
    }
    else {
        console.log(false);
    }

}