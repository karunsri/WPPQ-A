let arr = [1, 4, 6, 7, 8, 9, 23, 10];
let sum = 0;
for (let i = 0; i < arr.length; i++) {
    //     console.log(arr[i]);
    // }// print array

    //console.log(arr.length);// print array's length
    sum+=arr[i];
}
console.log(sum);
